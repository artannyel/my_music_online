# Task 2: Data - Repositório ArtistRepository (dart_ytmusic_api)

## 📌 Descrição Aprofundada
Obter dados completos do artista via método `getArtist(artistId)` do `dart_ytmusic_api`.

## 🎯 Escopo da Task
1. Criar `lib/src/features/artist/data/repositories/ytmusic_artist_repository.dart`.
2. Implementar `getArtist(artistId)` e mapear o retorno para `ArtistModel`.
3. Implementar endpoints secundários para as telas de "Ver Todos": `getArtistSongs()`, `getArtistAlbums()` e `getArtistSingles()`.

## 📋 Arquivos a Modificar / Criar
- `lib/src/features/artist/data/repositories/ytmusic_artist_repository.dart`

## ✅ Critérios de Aceite
- Retorno confiável das faixas e álbuns do artista.
